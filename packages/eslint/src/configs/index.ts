/* eslint-disable simple-import-sort/imports */
import { config as imports } from './import';
import { config as typescript } from './typescript';
import { config as node } from './node';
import { config as browser } from './browser';
import { config as react } from './react';
import { config as vue } from './vue';

export default {
  imports,
  typescript,
  node,
  browser,
  react,
  vue
};
